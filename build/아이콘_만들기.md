# 아이콘 재생성 방법

1. `build/icon.svg` 수정 (지오메트리: 88px 여백 + 848×848 라운드 사각형 rx=190 — PB 아이콘과 동일)
2. `qlmanage -t -s 1024 -o . icon.svg` 로 PNG 렌더
   ⚠️ **qlmanage 는 투명 배경을 흰색으로 채운다** — 반드시 3단계 후처리 필요
3. 라운드 바깥을 투명으로 뚫기: 저장소 루트에서 `node build/mask-icon.mjs`
4. 결과 확인: 모서리 픽셀 알파가 0 이어야 Dock 에서 흰 판이 안 생긴다
